{
  "name": "@kodranni/edge",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/handler.ts"
  },
  "scripts": {
    "sync-public": "node ./sync-public.mjs",
    "deploy": "node ./sync-public.mjs && wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.9"
  }
}
